# Grafana dashboard provisioning configuration
apiVersion: 1

providers:
  - name: 'SMEDREC Audit Server'
    orgId: 1
    folder: 'SMEDREC'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
